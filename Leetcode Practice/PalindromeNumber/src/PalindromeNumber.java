/**
 * Determine whether an integer is a palindrome. An integer is palindrome when it reads the same backward as forward
 * Example 1: input 121; output true;
 * Example 2: input -121; output false;
 * Example 3: input 10; output false;
 * Better solution would be the one without converting the integer to String.
 */

public class PalindromeNumber {
    public boolean isPalinDrome(int x) {

    }
}
